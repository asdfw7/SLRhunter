name: slrhunter
channels: [conda-forge, bioconda]
dependencies:
  - python=3.10
  - pip
  - kmc
  - bwa
  - gemma
  - biopython>=1.79
  - numpy>=1.20
  - pandas>=1.3
  - scipy>=1.7.0
  - psutil>=7.0.0
  - xopen>=1.0.0
  - pip:
      - .
